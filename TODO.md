# TODO

- [ ] Update UI to be terminal-size aware (compute cols/rows, progress bar width, dynamic rows)
- [ ] Remove hard-coded widths/row numbers from `ui_draw_frame`, `ui_update_session`, `ui_update_calendar`
- [ ] Wire layout refresh into `main.cpp` so UI redraw adapts to resizing
- [ ] Build + run smoke test; verify rendering does not overflow in small terminals

